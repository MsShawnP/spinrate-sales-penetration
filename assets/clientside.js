/**
 * clientside.js — Client-side callbacks for Spin Rate.
 * Handles click-to-pin interactions, opacity dimming, and number animations.
 * Populated in U3 (quadrant chart view).
 */

window.dash_clientside = Object.assign({}, window.dash_clientside, {
    spinrate: {}
});
