# Cinderhaven SSOT — Fly.io Postgres (iad region)
DATABASE_URL=postgres://user:password@host:5432/cinderhaven

# HTTP Basic Auth — required, app will not start without these.
DASH_AUTH_USERNAME=changeme
DASH_AUTH_PASSWORD=changeme
